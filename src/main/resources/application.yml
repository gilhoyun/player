spring:
  application:
    name: demo
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/SB_AM?
    username: root
    password:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: mailtestcjh@gmail.com
    password: xohvhfpztqxdzaqo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            
  # 카카오 로그인 설정 추가
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 0adf0657dcb2255caf8aec036b66bd0b  # 카카오 개발자 포털에서 받은 REST API 키
            client-secret: PF219vQNnCmmtt3CVojRkwKyE4tD5AGB  # 카카오 개발자 포털에서 받은 Client Secret
            redirect-uri: http://localhost:8080/usr/kakao/login  # 예: http://localhost:8080/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            client-name: Kakao
            scope:
              - profile_nickname
              - profile_image
              - account_email
              
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id